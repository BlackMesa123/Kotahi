version = "1.2.2"

plugins {
    id("com.android.library")
}

android {
    namespace = "com.samsung.android.view.animation"
    compileSdk = 32

    defaultConfig {
        minSdk = 24
        targetSdk = 32
    }
}